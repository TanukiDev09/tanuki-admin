# MongoDB Connection
# Replace with your actual connection string
# In Vercel, make sure to include the database name before the query parameters
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/accounting?retryWrites=true&w=majority

# JWT Authentication
# Use a strong, random string for production
JWT_SECRET=your-secret-key-change-this-in-production

# Environment
NODE_ENV=development

# Vercel Blob Storage (Required for book covers)
BLOB_READ_WRITE_TOKEN=your-blob-read-write-token
